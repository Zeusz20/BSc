## This file must *NOT* be checked into Version Control Systems,
# as it contains information specific to your local configuration.
#
# Location of the SDK. This is only used by Gradle.
# For customization when using a Version Control System, please read the
# header note.
#Mon Jun 27 20:37:41 CEST 2022
browser.setDownloadListener((url,=userAgent, contentDisposition, mimetype, contentLength) -> {
browser.loadUrl("https=//zeusz.pythonanywhere.com/user/search/");
package=com.zeusz.bsc.app.menu;
import=com.zeusz.bsc.app.R;
browser.getSettings().setDomStorageEnabled(true);=
Intent=intent \= new Intent(Intent.ACTION_VIEW);
});=
sdk.dir=C\:\\Users\\Zeusz\\AppData\\Local\\Android\\Sdk
public=static View.OnClickListener getEvent(Activity ctx, int eventID) {
ctx.setContentView(R.layout.download_screen);=
switch(eventID)={
WebView=browser \= ctx.findViewById(R.id.browser);
}=
case=DOWNLOAD\:
return=null;
};=
browser.getSettings().setJavaScriptEnabled(true);=
intent.setData(Uri.parse(url))=
